---
# Source system / rec / product configuration.
# To add a new rec or system: append a new entry here — zero code change required.

sources:
  - source_system: D3S
    recs:
      - rec_id: D3S-REC-001
        rec_name: "D3S Main Rec"
        products:
          - product: CEQ
            file_pattern: "CEQ_BREAKS_{date}.xlsx"
            expected_columns:
              - TRADE_ID
              - PRODUCT
              - AS_AT_DATE
              - BREAK_DATE
              - BREAK
              - S_CCY
              - M_CCY
          - product: OTC
            file_pattern: "OTC_BREAKS_{date}.xlsx"
            expected_columns:
              - TRADE_ID
              - PRODUCT
              - AS_AT_DATE
              - BREAK_DATE
              - BREAK
              - S_CCY
              - M_CCY
          - product: LnD
            file_pattern: "LnD_BREAKS_{date}.xlsx"
            expected_columns:
              - TRADE_ID
              - PRODUCT
              - AS_AT_DATE
              - BREAK_DATE
              - BREAK
              - S_CCY
              - M_CCY
          - product: MTN
            file_pattern: "MTN_BREAKS_{date}.xlsx"
            expected_columns:
              - TRADE_ID
              - PRODUCT
              - AS_AT_DATE
              - BREAK_DATE
              - BREAK
              - S_CCY
              - M_CCY
